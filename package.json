{
  "name": "shopify-ebook-generator",
  "version": "1.0.0",
  "description": "Generates a personalized PDF from a Shopify order.",
  "main": "api/generate-ebook.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@pdf-lib/fontkit": "^1.1.1",
    "pdf-lib": "^1.17.1",
    "resend": "^6.9.4"
  }
}
